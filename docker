# Быстрые команды docker compose для SandboxMCP
# запуск / обновление сервисов
cd /root/sandboxmcp && make compose-up

# проверка статуса
cd /root/sandboxmcp && docker compose ps

# просмотр логов
cd /root/sandboxmcp && make compose-logs

# остановка стеков
cd /root/sandboxmcp && make compose-down
